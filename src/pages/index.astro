---
import { DraftGame } from "../components/DraftGame";
import { loadDataset } from "../data";
import Layout from "../layouts/Layout.astro";

const dataset = loadDataset();
---

<Layout>
	<DraftGame client:load dataset={dataset} />
</Layout>
